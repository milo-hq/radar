# Global opportunity discovery

The user approved global solo-developer opportunity discovery with multilingual inputs and Chinese output. Geography must not restrict normal tool opportunities. Localization is one opportunity type, with independent origin/target evidence requirements.

Persist a versioned scan policy: audience global, Chinese report language, rotating search languages and review storefronts. Keep English coverage each round and add non-English searches without replacing the baseline. Search language and storefront are collection parameters, never evidence of a user's residence. Historical plans/results remain immutable.

Evidence context review records original language and an optional explicitly stated user market with an exact source line. Missing geography remains unknown. Reports derive observed markets from cited evidence; target applicability remains a hypothesis. Remove the US-to-three-region restriction and biased localization form defaults.

Add optional official YouTube and V2EX collection adapters, with credential-dependent scheduling and visible source readiness. Capture comments/replies with parent/thread provenance and bounded pagination; never treat unavailable credentials as zero demand. Other proposed platforms remain explicitly unconnected, not pretend collectors. Do not purchase access or silently broaden browser permissions.

Verification covers multilingual rotation, legacy compatibility, market attribution distinct from language/storefront, source scheduling and adapter pagination/provenance/errors. Deploy only after tests/typecheck/build. Existing browser permissions continue to work.
