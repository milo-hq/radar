# 2026-09-12 主题发现与数据源扩展

- 新增GitHub公开Issues、Stack Overflow问题，HN支持脱离产品的主题评论搜索。每渠道每次最多30条，最近两年，英文示例主题invoice、email automation、data export。
- 真实运行12个采集任务全部成功：首轮9个任务新增260条，GitHub标题匹配/按评论数排序补充3次新增90条。去重原文从90增至440：GitHub171、HN174、Stack Overflow90、官网5。没有生成虚构材料，没有自动接受痛点或商业证据。
- 测试：13项单元、30项数据库/API/研究/翻译、3项浏览器回归通过；构建及类型检查通过，Docker migration009部署完成。
- 独立审查修复：渠道锁固定排序避免并发死锁；数据库原子预约35秒请求间隔，保留更长API退避，延后任务不消耗尝试次数。单个渠道失败不影响其他渠道。
- 桌面1440、手机390无页面横向溢出。实测主题记录打开30条结果，点击结果进入原文检查器；一次临时检查使用了不存在的“原文”文本选择器，改用命名dialog后通过。截图.local/topic-discovery.png。
- 材料包括已关闭Issue、有回答问题和搜索噪声，页面明确显示这些状态；正文不含完整回复。保留作者、时间、原文URL、许可和搜索来源。可人工将GitHub项目加入参考产品关联材料。Reddit仍需配置，未接入产品评论站，技术社区覆盖偏差仍存在。

# 2026-09-12 来源覆盖与机会比较

- 来源统计核实：官网5个独立页面/10版本、HN85条评论、Reddit0（未配置）、手动网页0，参考产品3个。没有声称新增数据渠道。
- 新增机会比较后台任务、持久化输入输出快照、5维评分理由、已知权重覆盖和暂定验证排序。创始人资料空时匹配未知；更改机会/审核/资料/规则后比较过期。
- 11项单元测试、28项数据库/API/研究/翻译测试通过；3项原有浏览器回归通过。新增浏览器实测1440与390宽度下页面无横向溢出，比较表内部滚动。
- 审查修复：超过30项执行上限仍能读取旧比较并标记过期。低信息不排名、同分并列、租约失效不能提交、手工阻断覆盖高分均有验证。
- 真实12项评估暴露模型能力限制：即使明确要求，模型仍可将已有非营利折扣误判为差异化。增加持久化「优先级阻断原因」并按官网报价原文阻断该草稿；模型评估必须核对，不能替代事实审查。未确认痛点时提示先做不写代码的需求验证。

# 2026-09-12 剩余痛点与反馈发现增量

- 新增现有能力、剩余痛点、未解决原因、独立开发补充方案四项；模型推断明确标为待验证。
- HN 自动反馈发现上线：Buttondown 40 条、Buffer 8 条、Plausible 37 条，共新增 85 条真实讨论，均保留作者、时间、原文链接和上下文缺失标记。
- 三个真实研究任务成功，新增 5 个待核对草稿；没有将讨论或草稿自动标记为需求验证通过。生成结果仍有泛化、可能已被产品覆盖的假设，必须核对实际功能与用户原文。
- 回归：10 项核心/采集测试、27 项数据库/API/研究/翻译测试、3 项浏览器测试通过；类型检查、构建与 Docker 迁移 007 部署成功。
- 独立审查修复：产品来源单独保留采样配额；共享平台使用产品路径匹配，避免同域名串入其他产品材料。研究模型输入补充发表时间与上下文状态，后续提取限制官网声明占比。
- 浏览器确认反馈发现按钮、真实材料列表及新增计数；截图 `.local/feedback-discovery.png`。自动反馈发现当前仅覆盖 HN，研究每次只抽取最多 8 篇，采集量不等于已验证需求量。

# 2026-09-12 机会工作台重写验收

本节是当前状态；下方保留早期 Foundation 的历史记录。

- 核心规则 8 项、数据库/API/研究/翻译 26 项测试通过，类型检查和生产构建通过。
- Playwright 3 项通过：原文检查与移动布局、中文缓存原文对照、证据审核→验证决策→撤回证据仍保留历史。
- 数据模型追加迁移 004/005/006；原文、历史产品与译文保留。生产库迁移前已备份至忽略目录 .local/backups。
- Backend 独立审阅发现并修复长摘录索引上限、换行切分、研究与任务成功非原子提交。补充机会级证据审核，避免新方案继承旧声明的准入。
- 真实研究使用 gpt-4.1-mini 对 3 个已有产品运行，最终 3 个任务成功，保存 36 条声明、7 个机会草稿。候选为 0；未人工接受任何模型声明。
- 实测发现模型混淆原文编号/声明下标，改为两阶段固定 C0/C1 声明标识。模型仍可能生成范围过宽或语义支持不足的声明，因此所有输出待核对。
- 一条官网营销陈述被错误提取为 pain，已排除并写审计。后续对产品官网自动提取的痛点归为产品方市场陈述；不会自动满足独立需求门槛。
- 机会方案是研究假设；缺口统一标注待验证，缺需求证据明确提示。未实现自动全网发现、自动 SEO 调查或实验执行，不声称完成原始 V1 全部里程碑。

---

# Foundation Alpha 验证记录

日期：2026-09-11。工作目录：`/Users/huangqin/Documents/radar`。

## 可运行交付

Docker Compose 已实际启动：API、Worker、PostgreSQL 17 + pgvector。基础迁移与 vector 迁移通过。访问地址：http://127.0.0.1:4317 。原计划 4310 被 QQ 占用，因此项目改用 4317，未停止或修改 QQ。

本机 PostgreSQL 17（55432）用于独立数据库测试。市场数据在 Docker 的 `radar` 数据库；测试 fixture 在本机 `radar_test`，两者隔离。

## 已执行验证

| 检查 | 结果 |
| --- | --- |
| `npm test` | 8 个核心规则测试通过 |
| `npm run test:db` | 14 个 Postgres/API 集成测试通过 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，JS 约 255 KB / gzip 81 KB |
| `npx playwright test` | 1 个完整浏览器流程通过 |
| `npm audit` | 0 vulnerabilities |
| `docker compose up -d --build` | API/Worker 正常运行，DB healthy |
| pgvector migration | 已应用，运行时返回 vectorAvailable=true |
| 真实公开网页采集 | 3 个产品，5 个独立来源文档，10 个不可变版本快照 |

浏览器验证包含：关键词筛选、证据 drawer、审核保存及再次读取、无效 Reddit JSON 错误、Founder 配置刷新后持久化、手机导航、390px 无水平溢出、无 JS pageerror。测试自行创建 fixture，只写测试数据库。1440px 桌面截图和 390px 手机截图已目视检查。

关键后台约束覆盖：原文 UPDATE/DELETE 拒绝、重复导入幂等、变更追加快照、批量错误全部回滚、竞争领任务只有一个成功、旧租约无法完成、失败退避和最大次数、LLM 禁用失败审计且成本为 null、Reddit 父子/线程一致性、大线程根帖保留及显式截断、产品重定向别名合并、里程碑不重复计算新快照。

独立代码审查发现的三个问题已修复，并补回归测试：跨线程 parent_id、产品域名重定向去重、300 条 context 截断丢根帖。网页块级段落分隔也已修正；已有旧版本保持不可变，新抓取使用新解析器。

## 实际市场数据

成功采集：

- Plausible Analytics：https://plausible.io/
- Buffer：https://buffer.com/pricing 与 https://buffer.com/open
- Buttondown：https://buttondown.com/ 与 https://buttondown.com/pricing

5 个来源均保留标题、正文、URL、采集时间和原始 HTML。首次采集后改进段落提取，再次采集生成新快照，因此有 10 个版本；里程碑仍计 5 个来源文档。

尝试 `https://plausible.io/open` 返回 404，4 次有限重试后 failed。失败记录保留，没有作为产品证据入库；后续 smoke 脚本移除此失效地址。

当前：Reddit 原文 0；收入信号 0；人工接受 0；机会 0。生产库未导入测试 fixture、模型生成正文或虚构收入。抓到定价页不代表付费用户或收入已获验证。

## 尚未达成的门禁

- Milestone 1 的 200 真产品 / 500 真市场文档（含 Reddit）尚未达到。
- 未提供 Reddit OAuth token；OAuth 网络路径未做真实账户验证。线程 JSON 解析与导入已测试，但不等于实时 Reddit 已连通。
- 真实输入尚需 founder 人工审核；质量门禁保持关闭。
- 痛点抽取、embedding/semantic judge、Problem Clusters、SEO 分析、机会评分、Deep Research、Bull/Bear/Judge、Validation Plan 与 KILL 业务流程尚未实现。
- LLM 是接口、禁用 adapter 与审计基础，prompt 是版本化基础约束，不是可投入生产的角色工作流。
- 静态 HTML 抽取不会执行页面 JavaScript，复杂动态定价内容需要检查；不自动解决访问挑战。网站快照每次可变，内容去重不等同于语义或来源独立性判定。

因此本次结论是 **Foundation Alpha 可运行**，不是完整 V1 验收通过。

## 中文阅读助手增量（2026-09-11）

新增中文全文、带原文摘录的关键信息、缓存/续传、中文标题/正文搜索。`003_chinese_translation.sql` 已在 Docker 市场库与独立测试库应用。Docker API/Worker 已更新，真实环境 `/api/translation-config` 返回 configured=false；市场库未写入测试译文。

9 个翻译专项测试通过：Unicode 无损分段、虚假引用拒绝、原文不变、缓存命中、失败续传、模型截断拒绝、中文搜索、入队幂等和凭据隔离、配置错误不阻断已有译文、兼容接口到持久化的完整适配路径（一个测试覆盖多个约束）。2 个浏览器流程通过，其中一个核对中文要点、原文摘录和原始正文切换；使用独立测试库和明确的 fixture 模型名。类型检查与生产构建通过。

独立审查发现并修复：已有缓存不能依赖模型配置合法性；翻译任务的最大两次尝试必须与入队在同一事务提交。

真实模型调用和翻译语义质量尚未验证，因为服务商密钥和模型名称仍为空。详见 `docs/chinese-reading.md`。
